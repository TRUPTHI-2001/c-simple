
#include <stdio.h>
#include<stdlib.h>
void main()
{
    int a[50];
    int i,j,n;
    printf("enter the size of the array\n");
    scanf("%d",&n);
    printf("enter array elements\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(a[i]==a[j])
            {
                printf("non distint array");
                exit(0);
            }
        }
        
    }
      printf(" distint array");
}
